
version: "3.9"

services:

  backend:
    build:
      context: ./backend
    ports:
      - "8000:5001"

  frontend:
    build:
      context: ./templates/usecar_ki_system
    ports:
      - "80:80"
    depends_on:
      - backend