
class Sizes {

  static const double Small   = 8;
  static const double medium  = 16;
  static const double xMedium = 26;
  static const double large   = 32;

  // ── Spacing ───────────────────────────────────────────────────────────────
  static const double spacingSmall   = 8;
  static const double spacingMedium  = 16;
  static const double spacingXMedium = 26;
  static const double spacingLarge   = 32;

  // ── Insets ────────────────────────────────────────────────────────────────
  static const double insetXsmall  = 4;
  static const double insetSmall   = 8;
  static const double insetMedium  = 16;
  static const double insetXMedium = 16;
  static const double insetsLarge  = 24;
  static const double insetXLarge  = 32;

  // ── Border radius ─────────────────────────────────────────────────────────
  /// Card widgets (VehicleCard, DamagedReportPage, PricePredictionChart, result card)
  static const double radiusCard   = 16;
  /// Elevated buttons (predict button)
  static const double radiusButton = 14;
  /// Badge pills (damage count, no-damage badge)
  static const double radiusBadge  = 20;
  /// Damage tiles and image clip corners
  static const double radiusTile   = 10;
  /// Small chips, bar chart tops, zone bar corners, price badge
  static const double radiusChip   = 6;
  /// Pill-shaped search bar
  static const double radiusSearch = 25;
  /// Input fields (TextField, DropdownButtonFormField)
  static const double radiusInput  = 8;
  /// Dropdown and micro UI elements
  static const double radiusSmall  = 4;
  /// Legend dots in charts
  static const double radiusXSmall = 3;
  /// Price chart indicator tick
  static const double radiusTick   = 2;
}
